---
{{- if .Values.secret.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
type: Opaque
data:
  # Exemple : DB password (base64)
  db_password: {{ .Values.secret.db_password | default "" }}
{{- end }}
...
